=== AWS EKS Access Credentials ===
Created: ${timestamp}

User Information:
-----------------
Username:    ${username}
ARN:         ${user_arn}
Region:      ${region}

Programmatic Access:
--------------------
AWS_ACCESS_KEY_ID = ${access_key_id}
AWS_SECRET_ACCESS_KEY = ${secret_access_key}

Security Notice:
• These credentials provide access to AWS resources
• Store them securely - treat like passwords
• Rotate keys regularly (every 90 days recommended)
• Do not commit to version control or share via email